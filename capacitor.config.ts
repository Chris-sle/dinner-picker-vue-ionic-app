import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DinnerPicker-Vue/IonicApp',
  webDir: 'dist'
};

export default config;
